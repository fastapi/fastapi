name: FastAPI People

on:
  schedule:
    - cron: "30 * * * *"
  workflow_dispatch:

jobs:
  fastapi-people:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/actions/people
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
