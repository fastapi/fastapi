language: python

cache: pip

python:
    - "3.6"
    - "3.7-dev"

install:
    - pip install flit
    - flit install

before_script:
    - psql -c 'create database fastapi;' -U postgres

script:
    - bash scripts/test.sh

after_script:
    - bash <(curl -s https://codecov.io/bash)
